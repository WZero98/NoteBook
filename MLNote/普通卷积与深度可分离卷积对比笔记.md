# 普通卷积与深度可分离卷积对比笔记

本文对比 **普通卷积（Standard Convolution）** 与 **深度可分离卷积（Depthwise Separable Convolution）**，说明两者在计算方式、可学习参数量上的差异，并附上可用于比较实验的脚本。

---

## 1. 两者卷积的区别

### 1.1 普通卷积

普通卷积用一个三维卷积核（空间尺寸 \(K \times K\)，通道维覆盖全部输入通道 \(C_{in}\)）在输入特征图上滑动：

- **一次操作同时完成**：
  - 空间邻域聚合（spatial filtering）
  - 跨通道混合（channel mixing）
- 每个输出通道对应一组完整的 \(C_{in}\) 个 \(K \times K\) 滤波器，再对各通道结果求和（可加 bias）。

示意（忽略 stride / padding）：

```text
输入:  (N, C_in, H, W)
卷积核: (C_out, C_in, K, K)
输出:  (N, C_out, H', W')
```

在 PyTorch 中对应：

```python
nn.Conv2d(in_channels, out_channels, kernel_size=K, groups=1)
```

### 1.2 深度可分离卷积

深度可分离卷积把「空间滤波」和「通道混合」拆成两步（MobileNet 等轻量网络常用）：

| 步骤 | 名称 | 作用 | PyTorch 典型写法 |
|------|------|------|------------------|
| 1 | Depthwise（逐通道卷积） | 每个输入通道单独做 \(K \times K\) 空间卷积，不做跨通道混合 | `nn.Conv2d(C_in, C_in, K, groups=C_in)` |
| 2 | Pointwise（逐点卷积） | 用 \(1 \times 1\) 卷积把 \(C_{in}\) 个通道线性组合到 \(C_{out}\) | `nn.Conv2d(C_in, C_out, kernel_size=1)` |

示意：

```text
输入:           (N, C_in, H, W)
Depthwise:      (N, C_in, H', W')   # 通道数不变
Pointwise 1x1:  (N, C_out, H', W')  # 改变通道数
```

### 1.3 核心差异一览

| 对比项 | 普通卷积 | 深度可分离卷积 |
|--------|----------|----------------|
| 结构 | 单次 `Conv2d` | Depthwise + Pointwise |
| 空间滤波 | 有 | 有（depthwise） |
| 通道混合 | 与空间滤波同时完成 | 由 \(1\times1\) pointwise 单独完成 |
| 表达能力 | 通常更强（耦合空间与通道） | 参数更少，表达力相对受限，常需更多层/宽度补偿 |
| 典型用途 | ResNet、VGG、U-Net 等 | MobileNet、Xception、轻量检测/分割骨干 |

> **名词说明**：口语里说的「深度卷积」常指 **Depthwise Convolution**；完整的 **Depthwise Separable Convolution** = Depthwise + Pointwise。下文参数量对比针对完整的深度可分离卷积。

---

## 2. 可学习参数量的计算与比较

记：

- \(C_{in}\)：输入通道数  
- \(C_{out}\)：输出通道数  
- \(K\)：卷积核边长（正方形核）  
- 是否使用 bias：有 bias 时，普通卷积多 \(C_{out}\)；深度可分离在 depthwise 多 \(C_{in}\)、pointwise 多 \(C_{out}\)

### 2.1 普通卷积参数量

\[
P_{\text{std}} = K^{2} \cdot C_{in} \cdot C_{out} + \underbrace{C_{out}}_{\text{可选 bias}}
\]

无 bias 时：\(P_{\text{std}} = K^{2} C_{in} C_{out}\)。

### 2.2 深度可分离卷积参数量

**Depthwise**（`groups = C_in`，输出通道仍为 \(C_{in}\)）：

\[
P_{\text{dw}} = K^{2} \cdot C_{in} + \underbrace{C_{in}}_{\text{可选 bias}}
\]

**Pointwise**（\(1\times1\)）：

\[
P_{\text{pw}} = 1^{2} \cdot C_{in} \cdot C_{out} + \underbrace{C_{out}}_{\text{可选 bias}} = C_{in} C_{out} + C_{out}
\]

合计（无 bias）：

\[
P_{\text{dwsep}} = K^{2} C_{in} + C_{in} C_{out}
\]

有 bias 时：

\[
P_{\text{dwsep}} = K^{2} C_{in} + C_{in} + C_{in} C_{out} + C_{out}
\]

### 2.3 参数量比值（无 bias）

\[
\frac{P_{\text{dwsep}}}{P_{\text{std}}}
= \frac{K^{2} C_{in} + C_{in} C_{out}}{K^{2} C_{in} C_{out}}
= \frac{1}{C_{out}} + \frac{1}{K^{2}}
\]

结论：

- \(C_{out}\) 越大，或 \(K\) 越大，深度可分离卷积相对普通卷积越省参数。
- 例如 \(K=3,\, C_{out}=64\)：比值 \(\approx 1/64 + 1/9 \approx 0.1267\)（约 **12.7%**）。
- 例如 \(K=5,\, C_{out}=128\)：比值 \(\approx 1/128 + 1/25 \approx 0.0478\)（约 **4.8%**）。

### 2.4 数值示例（无 bias）

取 \(C_{in}=64,\, C_{out}=128,\, K=3\)：

| 类型 | 计算公式 | 参数量 |
|------|----------|--------|
| 普通卷积 | \(3^{2} \times 64 \times 128\) | \(73728\) |
| Depthwise | \(3^{2} \times 64\) | \(576\) |
| Pointwise | \(64 \times 128\) | \(8192\) |
| 深度可分离合计 | \(576 + 8192\) | \(8768\) |
| 比值 | \(8768 / 73728\) | \(\approx 0.119\) |

与公式 \(1/128 + 1/9 \approx 0.1189\) 一致。

### 2.5 与计算量（MAC）的关系

单次前向、输出空间尺寸为 \(H' \times W'\) 时（无 batch）：

- 普通卷积 MAC：\(K^{2} C_{in} C_{out} H' W'\)
- 深度可分离 MAC：\(K^{2} C_{in} H' W' + C_{in} C_{out} H' W'\)

MAC 比值同样约为 \(\dfrac{1}{C_{out}} + \dfrac{1}{K^{2}}\)。  
实际 GPU 时延还受 kernel 启动、带宽与实现优化影响，不一定严格等于理论 MAC 比。

---

## 3. 实验脚本说明

- 用 PyTorch 实现 `StandardConv` 与 `DepthwiseSeparableConv`
- 统计参数量、理论 MAC，并在 CPU/CUDA 上测前向时延与吞吐

```python
"""对比普通卷积与深度可分离卷积（Depthwise Separable Conv）。

依赖: torch（本脚本仅使用 PyTorch 官方模块）
运行: python compare_conv_vs_dwconv.py
"""

from __future__ import annotations

import sys
import time
from dataclasses import dataclass

import torch
import torch.nn as nn

if sys.platform == "win32":
    try:
        sys.stdout.reconfigure(encoding="utf-8")
    except Exception:
        pass


# ---------------------------------------------------------------------------
# 模块定义
# ---------------------------------------------------------------------------


class StandardConv(nn.Module):
    """普通卷积：一次 Conv2d 同时完成空间滤波与通道混合。"""

    def __init__(
        self,
        in_channels: int,
        out_channels: int,
        kernel_size: int = 3,
        stride: int = 1,
        padding: int | None = None,
        bias: bool = True,
    ) -> None:
        super().__init__()
        if padding is None:
            padding = kernel_size // 2
        self.conv = nn.Conv2d(
            in_channels,
            out_channels,
            kernel_size=kernel_size,
            stride=stride,
            padding=padding,
            bias=bias,
        )

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        return self.conv(x)


class DepthwiseSeparableConv(nn.Module):
    """深度可分离卷积 = 逐通道卷积（depthwise）+ 逐点卷积（pointwise 1x1）。

    - Depthwise: groups=in_channels，每个输入通道单独做空间卷积
    - Pointwise: 1x1 卷积，把各通道特征线性混合到 out_channels
    """

    def __init__(
        self,
        in_channels: int,
        out_channels: int,
        kernel_size: int = 3,
        stride: int = 1,
        padding: int | None = None,
        bias: bool = True,
    ) -> None:
        super().__init__()
        if padding is None:
            padding = kernel_size // 2
        self.depthwise = nn.Conv2d(
            in_channels,
            in_channels,
            kernel_size=kernel_size,
            stride=stride,
            padding=padding,
            groups=in_channels,
            bias=bias,
        )
        self.pointwise = nn.Conv2d(
            in_channels,
            out_channels,
            kernel_size=1,
            stride=1,
            padding=0,
            bias=bias,
        )

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        x = self.depthwise(x)
        x = self.pointwise(x)
        return x


# ---------------------------------------------------------------------------
# 统计工具
# ---------------------------------------------------------------------------


def count_parameters(module: nn.Module) -> int:
    return sum(p.numel() for p in module.parameters() if p.requires_grad)


def conv_macs(k: int, cin: int, cout: int, hout: int, wout: int, groups: int = 1) -> int:
    """一次卷积的乘加次数（MAC）。"""
    return (k * k * cin // groups) * cout * hout * wout


def theoretical_macs(
    in_channels: int,
    out_channels: int,
    kernel_size: int,
    hout: int,
    wout: int,
    kind: str,
) -> int:
    if kind == "standard":
        return conv_macs(kernel_size, in_channels, out_channels, hout, wout)
    if kind == "dwsep":
        dw = conv_macs(kernel_size, in_channels, in_channels, hout, wout, groups=in_channels)
        pw = conv_macs(1, in_channels, out_channels, hout, wout)
        return dw + pw
    raise ValueError(kind)


def format_num(n: float) -> str:
    if n >= 1e9:
        return f"{n / 1e9:.3f} G"
    if n >= 1e6:
        return f"{n / 1e6:.3f} M"
    if n >= 1e3:
        return f"{n / 1e3:.3f} K"
    return f"{n:.0f}"


def format_bytes(n: float) -> str:
    units = ["B", "KB", "MB", "GB"]
    value = float(n)
    for unit in units:
        if value < 1024 or unit == units[-1]:
            return f"{value:.2f} {unit}"
        value /= 1024
    return f"{value:.2f} GB"


@dataclass
class BenchResult:
    name: str
    params: int
    macs: int
    latency_ms: float
    throughput: float
    peak_mem_bytes: int
    out_mean: float
    out_std: float


def sync_if_cuda(device: torch.device) -> None:
    if device.type == "cuda":
        torch.cuda.synchronize(device)


def benchmark(
    name: str,
    module: nn.Module,
    x: torch.Tensor,
    macs: int,
    warmup: int = 20,
    repeats: int = 50,
) -> BenchResult:
    device = x.device
    module = module.to(device).eval()

    with torch.no_grad():
        y = module(x)
        for _ in range(warmup):
            _ = module(x)
        sync_if_cuda(device)

        if device.type == "cuda":
            torch.cuda.reset_peak_memory_stats(device)
            mem_before = torch.cuda.memory_allocated(device)
        else:
            mem_before = 0

        start = time.perf_counter()
        for _ in range(repeats):
            y = module(x)
        sync_if_cuda(device)
        elapsed = time.perf_counter() - start

        if device.type == "cuda":
            peak_mem = torch.cuda.max_memory_allocated(device) - mem_before
            peak_mem = max(peak_mem, 0)
        else:
            peak_mem = 0

    latency_ms = elapsed / repeats * 1000
    throughput = x.shape[0] / (elapsed / repeats)
    return BenchResult(
        name=name,
        params=count_parameters(module),
        macs=macs,
        latency_ms=latency_ms,
        throughput=throughput,
        peak_mem_bytes=peak_mem,
        out_mean=float(y.mean().item()),
        out_std=float(y.std().item()),
    )


def print_table(rows: list[BenchResult], baseline: BenchResult) -> None:
    headers = ["模块", "参数量", "MAC", "时延(ms)", "吞吐(img/s)", "峰值显存", "相对参数", "相对MAC", "相对时延"]
    lines = []
    for r in rows:
        lines.append(
            [
                r.name,
                format_num(r.params),
                format_num(r.macs),
                f"{r.latency_ms:.3f}",
                f"{r.throughput:.1f}",
                format_bytes(r.peak_mem_bytes) if r.peak_mem_bytes else "-",
                f"{r.params / baseline.params:.3f}x",
                f"{r.macs / baseline.macs:.3f}x",
                f"{r.latency_ms / baseline.latency_ms:.3f}x",
            ]
        )

    widths = [max(len(h), *(len(row[i]) for row in lines)) for i, h in enumerate(headers)]

    def fmt_row(cols: list[str]) -> str:
        return " | ".join(c.ljust(widths[i]) for i, c in enumerate(cols))

    print(fmt_row(headers))
    print("-+-".join("-" * w for w in widths))
    for row in lines:
        print(fmt_row(row))


def theoretical_ratio(in_channels: int, out_channels: int, kernel_size: int) -> float:
    """深度可分离卷积相对普通卷积的理论 MAC 比：1/Cout + 1/K^2。"""
    return 1.0 / out_channels + 1.0 / (kernel_size ** 2)


# ---------------------------------------------------------------------------
# 主流程
# ---------------------------------------------------------------------------


def run_one_config(
    in_channels: int,
    out_channels: int,
    kernel_size: int,
    spatial: int,
    batch: int,
    device: torch.device,
) -> None:
    padding = kernel_size // 2
    hout = wout = (spatial + 2 * padding - kernel_size) // 1 + 1  # stride=1

    std = StandardConv(in_channels, out_channels, kernel_size)
    dw = DepthwiseSeparableConv(in_channels, out_channels, kernel_size)

    x = torch.randn(batch, in_channels, spatial, spatial, device=device)

    std_macs = theoretical_macs(in_channels, out_channels, kernel_size, hout, wout, "standard")
    dw_macs = theoretical_macs(in_channels, out_channels, kernel_size, hout, wout, "dwsep")

    print("=" * 88)
    print(
        f"配置: Cin={in_channels}, Cout={out_channels}, K={kernel_size}, "
        f"H=W={spatial}, batch={batch}, device={device}"
    )
    print(f"普通卷积结构     : Conv2d({in_channels}->{out_channels}, {kernel_size}x{kernel_size}) + BN + ReLU")
    print(
        f"深度可分离卷积结构: DW Conv2d(groups={in_channels}, {kernel_size}x{kernel_size}) "
        f"+ PW Conv2d({in_channels}->{out_channels}, 1x1) + BN + ReLU"
    )
    print(
        f"理论 MAC 比 (DWSep / Standard) ≈ 1/Cout + 1/K^2 "
        f"= {theoretical_ratio(in_channels, out_channels, kernel_size):.4f}"
    )
    print()

    std_res = benchmark("普通卷积", std, x, std_macs * batch)
    dw_res = benchmark("深度可分离卷积", dw, x, dw_macs * batch)
    print_table([std_res, dw_res], baseline=std_res)
    print()
    print(
        f"输出统计: 普通卷积 mean={std_res.out_mean:.4f} std={std_res.out_std:.4f} | "
        f"深度可分离 mean={dw_res.out_mean:.4f} std={dw_res.out_std:.4f}"
    )
    print("说明: 两模块权重独立随机初始化，输出统计仅用于确认前向可运行，不代表精度对比。")
    print()


def main() -> None:
    torch.manual_seed(0)
    if torch.cuda.is_available():
        torch.cuda.manual_seed_all(0)

    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    print(f"PyTorch {torch.__version__} | device = {device}")
    if device.type == "cuda":
        print(f"GPU: {torch.cuda.get_device_name(0)}")
    print()

    # 几组常见视觉骨干配置，便于观察通道数 / 核大小对收益的影响
    configs = [
        dict(in_channels=32, out_channels=64, kernel_size=3, spatial=56, batch=16),
        dict(in_channels=64, out_channels=128, kernel_size=3, spatial=28, batch=16),
        dict(in_channels=128, out_channels=256, kernel_size=3, spatial=14, batch=16),
        dict(in_channels=64, out_channels=64, kernel_size=3, spatial=56, batch=16),
        dict(in_channels=64, out_channels=128, kernel_size=5, spatial=28, batch=16),
    ]

    for cfg in configs:
        run_one_config(device=device, **cfg)

    print("=" * 88)
    print("结论要点")
    print("- 普通卷积一次完成空间聚合与通道混合，参数量和计算量约为 K^2 * Cin * Cout * H * W。")
    print("- 深度可分离卷积把两者拆开：depthwise 只做空间滤波，pointwise 只做通道混合。")
    print("- 理论计算量约为 Standard 的 (1/Cout + 1/K^2)；通道越多、卷积核越大，节省越明显。")
    print("- 实际时延还受 GPU kernel 启动、内存带宽、实现优化影响，不一定严格等于理论 MAC 比。")


if __name__ == "__main__":
    main()
    # x = torch.randn(1, 3, 1024, 1024)
    # std = StandardConv(3, 32, 3, padding=1)
    # dw = DepthwiseSeparableConv(3, 32, 3, padding=1)
    # y_std = std(x)
    # y_dw = dw(x)
    # print(y_std.shape)
    # print(y_dw.shape)
    # for name, p in std.named_parameters():
    #     if p.requires_grad:
    #         print(f"{name}: {p.shape}，参数量：{p.numel()}")
```

---

## 4. 小结

1. **普通卷积**：空间滤波与通道混合耦合在一次卷积中，参数多为 \(K^{2} C_{in} C_{out}\)。  
2. **深度可分离卷积**：先逐通道空间卷积，再用 \(1\times1\) 混合通道，参数约为 \(K^{2} C_{in} + C_{in} C_{out}\)。  
3. **相对参数量（无 bias）**：\(\dfrac{1}{C_{out}} + \dfrac{1}{K^{2}}\)，通道越多、核越大，优势越明显。  
4. 可用同目录脚本实测参数量、MAC 与前向时延，对照理论公式。
