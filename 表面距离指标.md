# 表面距离指标

> 三维图像分割中常用，二维同样适用。  
> 核心思想：不只看区域重叠（Dice/IoU），还看**边界对齐**得好不好。

**参考库**：[google-deepmind/surface-distance](https://github.com/google-deepmind/surface-distance)

| 指标 | 库内函数 |
|------|----------|
| Average Surface Distance | `compute_average_surface_distance` |
| Hausdorff Distance | `compute_robust_hausdorff` |
| Surface Overlap | `compute_surface_overlap_at_tolerance` |
| Surface Dice | `compute_surface_dice_at_tolerance` |
| Volumetric Dice | `compute_dice_coefficient` |

---

## 1. 公共前置：提取表面距离

DeepMind 库要求先算双向表面距离字典，再喂给各指标函数。掩码需为 `bool`，并传入体素间距 `spacing_mm`。

```python
import numpy as np
import surface_distance as sd

# mask_gt / mask_pred: bool 数组，形状可为 (H,W) 或 (D,H,W)
# spacing_mm: 与维度对应的物理间距，单位 mm
surface_distances = sd.compute_surface_distances(
    mask_gt.astype(bool),
    mask_pred.astype(bool),
    spacing_mm=(1.0, 1.0, 1.0),  # 3D 示例；(sy, sx) 用于 2D
)
# 返回字典含：
#   distances_gt_to_pred, distances_pred_to_gt
#   surfel_areas_gt, surfel_areas_pred
```

以下各节代码均假定已得到 `surface_distances`（或自行用距离变换实现等价逻辑）。

---

## 2. Average Surface Distance（ASD）

### 2.1 别称

- **ASD** / **AvgSD**：Average Surface Distance  
- **MSD**：Mean Surface Distance（部分文献与 ASD 混用）  
- **ASSD**：Average **Symmetric** Surface Distance（双向平均的对称版，见第 7 节）  
- **MASD**：Mean Average Surface Distance（先各向求均值再平均，与 ASSD 略有不同）

> 注意：不同论文/工具里 ASD、ASSD、MASD 定义不完全一致，写实验时最好标明公式或所用库。

### 2.2 定义与逻辑

- **定义**：两个分割表面之间对应点最短距离的**平均**，反映边界的整体偏差。  
- **核心逻辑**：分别算「预测→真实」「真实→预测」的点到表面最短距离，再按表面积加权取平均（DeepMind 实现会返回两个方向各自的均值）。  
- **适用**：评估整体边界精度；值越低越好。医学分割论文常与 Dice 并列报告。

### 2.3 代码示例

```python
# DeepMind：返回 (avg_gt_to_pred, avg_pred_to_gt)
asd_gt2pred, asd_pred2gt = sd.compute_average_surface_distance(surface_distances)
asd = (asd_gt2pred + asd_pred2gt) / 2.0  # 常用对称 ASD / 近似 ASSD

# 手写示意（基于距离变换，无面积加权；仅便于理解）
from scipy.ndimage import distance_transform_edt

def directed_avg_surface_distance(mask_a, mask_b, spacing=(1.0, 1.0, 1.0)):
    """从 mask_a 表面到 mask_b 表面的平均距离。"""
    # 到 mask_b 前景的距离；表面点取 mask_a 的边界
    dt = distance_transform_edt(~mask_b, sampling=spacing)
    # 简单边界：前景邻域有背景
    from scipy.ndimage import binary_erosion
    surf_a = mask_a & ~binary_erosion(mask_a)
    if not surf_a.any():
        return np.nan
    return float(dt[surf_a].mean())

asd_simple = 0.5 * (
    directed_avg_surface_distance(mask_gt, mask_pred)
    + directed_avg_surface_distance(mask_pred, mask_gt)
)
```

---

## 3. Hausdorff Distance（HD）

### 3.1 别称

- **HD** / **HDD**：Hausdorff Distance  
- **MHD**：Maximum Hausdorff Distance（强调取最大）  
- **HDp** / **HD95** / **HD99**：百分位稳健豪斯多夫（取第 *p* 百分位，常用 95）  
- **Directed HD**：单向豪斯多夫（仅 max of one direction）  
- **Modified HD (MHD)**：部分旧文献指平均最近邻距离，勿与 Maximum HD 混淆

### 3.2 定义与逻辑

- **定义**：两个表面之间距离的**最大值**，捕捉最不匹配的边界点。  
- **核心逻辑**：\( H(A,B)=\max\big( \sup_{a\in A}\inf_{b\in B}\|a-b\|,\ \sup_{b\in B}\inf_{a\in A}\|b-a\| \big) \)。  
- 对离群点极敏感，实践多用 **HD95**（距离分布的第 95 百分位）。  
- **适用**：手术导航、放疗靶区等需严格控制最大边界误差的场景。

### 3.3 代码示例

```python
hd100 = sd.compute_robust_hausdorff(surface_distances, percent=100)  # 经典 HD
hd95  = sd.compute_robust_hausdorff(surface_distances, percent=95)   # 常用稳健版

# 手写示意（无面积加权）
def hausdorff_distance(mask_a, mask_b, spacing=(1.0, 1.0, 1.0), percentile=95):
    from scipy.ndimage import binary_erosion, distance_transform_edt
    def surf(m):
        return m & ~binary_erosion(m)
    sa, sb = surf(mask_a), surf(mask_b)
    dt_b = distance_transform_edt(~mask_b, sampling=spacing)
    dt_a = distance_transform_edt(~mask_a, sampling=spacing)
    d_ab = dt_b[sa] if sa.any() else np.array([np.inf])
    d_ba = dt_a[sb] if sb.any() else np.array([np.inf])
    return float(max(np.percentile(d_ab, percentile), np.percentile(d_ba, percentile)))
```

---

## 4. Surface Overlap（表面重叠）

### 4.1 别称

- **Surface Overlap** / **SO**  
- 与 **Boundary IoU（BIoU）** / **边界交并比** 密切相关：均在容忍带内衡量边界重叠；DeepMind 的 Surface Overlap 返回两个方向各自的覆盖率，BIoU 则是交/并形式。  
- 有时也称 **Boundary Overlap**、**Tolerance-based Surface Overlap**

### 4.2 定义与逻辑

- **定义**：在给定容忍度 \(\tau\)（mm）内，一侧表面上落在另一侧容忍带内的面积占比。  
- **核心逻辑（BIoU 视角）**：对边界做 \(\pm\tau\) 膨胀后算交并比  
  \(\mathrm{BIoU}=\dfrac{|A_\tau \cap B_\tau|}{|A_\tau \cup B_\tau|}\)。  
- **适用**：允许一定标注/成像误差时，评估边界覆盖是否充分。

### 4.3 代码示例

```python
# DeepMind：返回 (overlap_gt, overlap_pred)，即各方向在 τ 内的表面比例
ov_gt, ov_pred = sd.compute_surface_overlap_at_tolerance(
    surface_distances, tolerance_mm=1.0
)
surface_overlap_mean = 0.5 * (ov_gt + ov_pred)

# BIoU 思路示意（形态学膨胀边界带）
from scipy.ndimage import binary_dilation, generate_binary_structure

def boundary_iou(mask_a, mask_b, tol_voxels=2):
    struct = generate_binary_structure(mask_a.ndim, 1)
    def band(m):
        from scipy.ndimage import binary_erosion
        surf = m & ~binary_erosion(m)
        return binary_dilation(surf, structure=struct, iterations=tol_voxels)
    ba, bb = band(mask_a), band(mask_b)
    inter = np.logical_and(ba, bb).sum()
    union = np.logical_or(ba, bb).sum()
    return float(inter / union) if union else np.nan
```

---

## 5. Surface Dice（表面 Dice）

### 5.1 别称

- **Surface Dice** / **Surface DSC**  
- **NSD**：Normalized Surface Distance / **Normalized Surface Dice**（Nikolov et al., 常与 Surface Dice 等价或极接近）  
- **NSC**：Normalized Surface Cube（少见写法）  
- **Tolerance Dice on Surfaces**

### 5.2 定义与逻辑

- **定义**：在容忍度 \(\tau\) 内，两侧表面“对齐”的面积占两侧表面积之和的比例，形式类似体积 Dice。  
- **公式**：  
  \(\mathrm{SurfaceDice}(\tau)=\dfrac{2\cdot |A\cap_\tau B|}{|A|+|B|}\)  
  （DeepMind 实现为两侧在 \(\tau\) 内重叠面积之和 / 两侧表面积之和。）  
- **适用**：比 Surface Overlap 更对称；对小目标边界更敏感；是 NSD 的常用实现基础。医学上 \(\tau\) 常取 1–3 mm。

### 5.3 代码示例

```python
surface_dice = sd.compute_surface_dice_at_tolerance(
    surface_distances, tolerance_mm=2.0  # NSD@2mm 常见设置
)
# 范围 [0, 1]，越大越好
```

---

## 6. Volumetric Dice（体积 Dice）

### 6.1 别称

- **DSC** / **Dice** / **Dice Similarity Coefficient**  
- **F1 Score**（二分类体素级与 Dice 等价）  
- **Sørensen–Dice Coefficient**  
- **Overlap Index**（广义重叠族的一员）

### 6.2 定义与逻辑

- **定义**：经典区域重叠指标，\(\mathrm{DSC}=\dfrac{2|X\cap Y|}{|X|+|Y|}\)。  
- **本质**：与表面距离无关，是**体积指标**。常与边界指标一起报：一个看“填得满不满”，一个看“边界准不准”。

### 6.3 代码示例

```python
vol_dice = sd.compute_dice_coefficient(mask_gt.astype(bool), mask_pred.astype(bool))

# 等价手写
intersection = np.logical_and(mask_gt, mask_pred).sum()
vol_dice = 2.0 * intersection / (mask_gt.sum() + mask_pred.sum() + 1e-8)
```

---

## 7. 笔记未列：其他常见表面 / 边界距离指标

| 指标 | 别称 | 要点 |
|------|------|------|
| **ASSD** | Average Symmetric Surface Distance | 两侧所有表面点距离的全局平均（对称）；与 ASD 常一起出现 |
| **MASD** | Mean Average Surface Distance | \(\frac{1}{2}(\overline{d}(A\!\to\!B)+\overline{d}(B\!\to\!A))\)，先定向平均再平均 |
| **RMSD / RMSSD** | Root Mean Square Surface Distance | 表面距离的均方根，对大误差比 ASD 更敏感 |
| **MSD（最大表面距离）** | Max Surface Distance | 部分实现指 HD；命名冲突多，需看原文公式 |
| **HDp** | Percentile Hausdorff | 一般化 HD95；p=100 即经典 HD |
| **BIoU** | Boundary IoU | 边界带交并比（Cheng et al., 2021），偏检测/分割边界质量 |
| **NSD** | Normalized Surface Dice/Distance | 相对指标（0–1），需指定 \(\tau\)；DeepMind Surface Dice 是其实装之一 |
| **Contour / Mean Contour Distance** | MCD | 2D 轮廓点列上的平均最近邻距离，超声/心脏轮廓常用 |
| **Symmetric Mean Absolute Surface Distance** | 同 ASSD 族 | 部分软件（如 ITK、MeVisLab）中的命名 |

```python
# ASSD / MASD / RMSD 手写示意（距离变换版）
from scipy.ndimage import binary_erosion, distance_transform_edt

def surface_distance_arrays(mask_a, mask_b, spacing=(1., 1., 1.)):
    def surf(m):
        return m & ~binary_erosion(m)
    sa, sb = surf(mask_a), surf(mask_b)
    d_ab = distance_transform_edt(~mask_b, sampling=spacing)[sa]
    d_ba = distance_transform_edt(~mask_a, sampling=spacing)[sb]
    return d_ab, d_ba

d_ab, d_ba = surface_distance_arrays(mask_gt, mask_pred)
assd = float(np.concatenate([d_ab, d_ba]).mean())          # ASSD
masd = 0.5 * (float(d_ab.mean()) + float(d_ba.mean()))     # MASD
rmsd = float(np.sqrt(np.mean(np.concatenate([d_ab, d_ba]) ** 2)))
```

---

## 8. 作为评价指标或损失函数的实例任务

### 8.1 作评价指标（Evaluation）

| 任务 / 基准 | 常用表面相关指标 | 说明 |
|-------------|------------------|------|
| **BraTS** 脑胶质瘤分割 | Dice + HD95 | 挑战赛与论文几乎标配；强调瘤周最大误差 |
| **LiTS / MSD 肝脏与肿瘤** | Dice + ASD / HD | 大器官体积 + 边界误差一并报告 |
| **SegTHOR / ACDC 心脏与胸腔器官** | Dice + HD / ASSD | 心腔、大血管边界对下游功能分析关键 |
| **放疗靶区 / OAR 勾画**（头颈、前列腺等） | HD95、ASD、Surface Dice/NSD | 剂量学对边界偏移敏感；DeepMind Surface Dice 即源于临床勾画一致性研究 |
| **ISLES / WMH 小病灶分割** | Dice + HD；常辅以边界指标 | 类别极不平衡，体积 Dice 易虚高，需看边界 |
| **Metrics Reloaded / 挑战赛协议** | NSD、BIoU、ASSD、HDp 等 | 推荐边界族与重叠族互补使用 |

### 8.2 作损失函数（Training）

| 损失 / 方法 | 关联指标 | 典型任务实例 |
|-------------|----------|--------------|
| **Hausdorff Loss**（Karimi et al., 2019） | 直接逼近 / 降低 HD | 前列腺超声、肝 CT、胰腺 CT；约可降 HD 18–45% 而不伤 Dice |
| **Boundary Loss**（Kervadec et al., MIDL 2019） | 轮廓空间距离，常改善 HD | **ISLES** 缺血卒中、**WMH** 白质高信号等极不平衡病灶 |
| **Generalized Surface Loss** | 面向 HD 族、带类别权重 | **LiTS**、**BraTS** 上与 nnU-Net 等骨干结合 |
| **Active Contour Loss**（Chen et al., CVPR 2019） | 边界长度 + 区域拟合，评测常用 HD | 心脏结构分割，约束轮廓光滑与形状 |
| **Perimeter / Contour-based Loss** | 周长或轮廓匹配 | 脾、海马、多器官分割，减少孔洞与边界毛刺 |
| **Dice + Boundary / HD 组合损失** | DSC + ASD/HD | 通用医学分割训练配方：区域损失稳住重叠，边界损失压最大误差 |

**选型直觉（简）：**

- 只要“平均贴边”好 → 看 **ASD / ASSD**，训练可配 Boundary Loss。  
- 不能有“离谱尖刺/漏勾” → 看 **HD95**，训练可配 Hausdorff / Surface Loss。  
- 标注本身有毫米级抖动 → 看 **Surface Dice / NSD@τ**，τ 取临床可接受误差。  
- 只报 Dice 时 → 大器官可能“重叠很高但边界仍歪”，务必搭配至少一种表面距离。

---

## 9. 一页速查

```text
重叠好不好？     → Volumetric Dice / IoU
平均边偏多少？   → ASD / ASSD / MASD（mm，↓更好）
最坏边偏多少？   → HD / HD95（mm，↓更好）
容忍带内贴边率？ → Surface Dice / NSD、Surface Overlap / BIoU（↑更好）
```
